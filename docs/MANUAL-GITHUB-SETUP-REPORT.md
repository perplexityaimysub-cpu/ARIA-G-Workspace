# گزارش رسمی راه‌اندازی گیت‌هاب دستی (MANUAL GITHUB SETUP REPORT)

این گزارش تأییدکننده اتصال ایمن و راه‌اندازی بستر همگام‌سازی دستی محیط کاری فعال ARIA-G به مخزن گیت‌هاب است.

## ۱. اطلاعات هویتی و مسیرها:
* **مسیر محیط کاری محلی (Workspace Path):** `C:\Users\Admin\Documents\ARIA-G-Workspace`
* **مخزن هدف گیت‌هاب (Target Remote):** `https://github.com/perplexityaimysub-cpu/aria-g-workspace.git`
* **وضعیت نصب Git:** نصب شده (سازگار با نسخه ویندوز)
* **شاخه فعال (Current Branch):** `main`
* **وضعیت اتصال Remote (Origin):** متصل (پاسخگو و فعال)
* **فایل پشتیبان قبل از راه‌اندازی:** `C:\Users\Admin\Documents\ARIA-G-Git-Backups\ARIA-G-Workspace-before-manual-git-20260716-225233.zip`
* **چک‌سام SHA-256 فایل پشتیبان:** `1dadfb3f1947073a651f7f3e6987fc5fd6fd5413ffd792732e487186b60860ed`

## ۲. وضعیت ارتباط تاریخچه محلی و سرور (Local vs Remote Relationship):
* **وضعیت فعلی:** `REMOTE_HAS_HISTORY_LOCAL_IS_UNTRACKED`
* **شرح وضعیت:** محتوای محلی زنگ اول در حال حاضر فاقد کامیت است (تغییرات به صورت Untracked هستند)، در حالی که سرور گیت‌هاب دارای تاریخچه ثبت‌شده از آپلودهای گذشته است. این دو تاریخچه هنوز به هم متصل نشده‌اند.

## ۳. طرح پیشنهادی اولین همگام‌سازی ایمن (First Safe Sync Plan):
به دلیل عدم اتصال تاریخچه‌ها، برای پیشگیری از بازنویسی کدهای محلی یا تداخل، فرآیند زیر برای اولین همگام‌سازی توصیه می‌شود:
1. نرم‌افزار **GitHub Desktop** را باز کنید.
2. پوشه محلی `C:\Users\Admin\Documents\ARIA-G-Workspace` را اضافه کنید.
3. در گام اول، فایل‌های جدید یا تغییر یافته خود را در تب `Changes` کامیت کنید (مثلاً با پیام "محتوای بومی جدید و تست‌های متفرقه").
4. پس از کامیت محلی، چون گیت‌هاب حاوی کدهای قدیمی است، با انجام Fetch متوجه تفاوت تاریخچه‌ها می‌شوید.
5. برای ادغام بدون بازنویسی، دستور زیر را به صورت دستی در پاورشل اجرا کنید تا تاریخچه گیت‌هاب بدون تداخل با تغییرات محلی شما همگام شود (اجازه دادن به تاریخچه‌های غیرمرتبط):
   ```powershell
   git pull origin main --allow-unrelated-histories
   ```
6. در صورت بروز تعارض در فایل‌های تکراری، فایل‌های محلی خودتان را حفظ کنید.
7. پس از حل تعارض محلی، تغییرات ادغام شده را Commit کرده و در نهایت `Push origin` کنید.

## ۴. بیانیه تأییدیه فنی و ایمنی:
* [x] هیچ‌گونه واچر (Watcher)، تسک برنامه‌ریزی‌شده (Scheduled Task) یا پردازش پس‌زمینه‌ای ساخته نشده است.
* [x] هیچ کامیت خودکار یا آپلود خودکاری در پس‌زمینه انجام نمی‌شود.
* [x] کنترل همگام‌سازی ۱۰۰٪ دستی و در اختیار اپراتور است.
* [x] برنامه‌ی GitHub Desktop اکنون قادر است این پوشه را به عنوان مخزن شناسایی و مدیریت کند.

## ۵. اقدام بعدی توصیه‌شده (Next Recommended Action):
باز کردن مخزن محلی در برنامه **GitHub Desktop**، انجام اولین کامیت تغییرات محلی فعال، و سپس اجرای دستور ادغام تاریخچه‌ها (`--allow-unrelated-histories`) در کنسول پاورشل طبق پروتکل فوق.
