---
name: r4-schema-entity-graph
description: Generates valid JSON-LD structured data schema blocks based on verified specifications.
---

# R4: Schema & Entity Graph Blueprint

## 1. Schema Generation
* Generates Product, Brand, FAQ, or LocalBusiness JSON-LD markup.
* **Checks:** Enforces E-006 image rules (no alt text or image schema if image asset is absent).
