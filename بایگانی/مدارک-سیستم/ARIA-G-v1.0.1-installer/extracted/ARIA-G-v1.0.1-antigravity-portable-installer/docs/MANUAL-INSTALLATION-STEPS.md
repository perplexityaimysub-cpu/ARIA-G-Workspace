# Manual Installation Steps
1. Create a backup of the active `.agents/skills/` directory.
2. Extract the payload skills under `payload/skills/` directly to `<TARGET_WORKSPACE>\.agents\skills\`.
3. Reload the Antigravity workspace customizations.
